# Required — Neon PostgreSQL
DATABASE_URL=

# Optional — live AI draft (Gemini)
# GEMINI_API_KEY=
# GEMINI_MODEL=gemini-2.5-flash
# GEMINI_MODEL_FALLBACK=gemini-2.5-flash-lite

# Optional — production seed endpoint
# SEED_SECRET=

# Optional — rate limiting (defaults in lib/rate-limit.ts)
# RATE_LIMIT_DAILY=10
# RATE_LIMIT_ENABLED=true
